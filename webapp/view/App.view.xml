<mvc:View  
	controllerName="com.wakefern.zflexcl.controller.App"
	displayBlock="true"
	height="100%"
	xmlns="sap.f"
	xmlns:mvc="sap.ui.core.mvc">
		<FlexibleColumnLayout id="flexibleColumnLayout" stateChange="onStateChanged"  backgroundDesign="Solid" layout="{/layout}">
		  <!-- remove the hard-coded beginColumnPages and endColumnPages aggregations (since the router will add them automatically from now on),
		 and we bind the layout property so that it can be changed easily from the controller. -->
		 <!-- <beginColumnPages>
			<mvc:XMLView id="beginView" viewName="com.wakefern.zflexcl.view.Master"/>
		</beginColumnPages>
		<midColumnPages>
			<mvc:XMLView id="detailView" viewName="com.wakefern.zflexcl.view.Detail"/>
		</midColumnPages>		  -->
	</FlexibleColumnLayout>
</mvc:View>
